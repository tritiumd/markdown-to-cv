[project]
name = "cv-tritiumd"
version = "1.0.0"
description = ""
authors = [
    {name = "delus",email = "kid.clone.3@gmail.com"}
]
readme = "README.md"
packages = [{include = "cv_tritiumd", from = "src"}]
requires-python = ">=3.12"
dependencies = [
    "fastapi (>=0.116.0,<0.117.0)",
    "uvicorn (>=0.35.0,<0.36.0)",
    "tex-corver-core @ git+ssh://git@github.com/tex-corver/core.git",
    "tex-corver-fastapi-utils @ git+ssh://git@github.com/tex-corver/fastapi-utils.git",
    "pydantic (>=2.11.7,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
